# Set this only when a real backend is deployed or running locally.
# Leave it empty to let the frontend fall back to demo mode in hosted environments.
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_TRUSTSPHERE_API_KEY=trustsphere-local-dev-key
