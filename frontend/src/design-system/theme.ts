import { stitchTokens } from './tokens';

export const trustSphereTheme = {
  id: 'trustsphere-stitch-dark',
  name: 'TrustSphere Stitch Dark',
  mode: 'dark',
  tokens: stitchTokens,
};

export type TrustSphereTheme = typeof trustSphereTheme;
